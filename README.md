Backend: Node.js, Express.js

View: Server-side rendered pages (views/) (เช่น EJS/Pug/ฯลฯ)

Database: Connected via db.js (เช่น MySQL)

File Upload: upload.js (เช่น Multer)

Others: Environment config via .env, static assets in public/

ทิปเล็กน้อย: .env อย่าลง GitHub ให้ใส่ไว้ใน .gitignore
